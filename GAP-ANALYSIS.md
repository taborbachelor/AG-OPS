# Gap Analysis vs ARCHITECTURE.md Directive

**Date:** 2026-07-22/23 · **Method:** three parallel line-level code audits (MAVLink/link layer; missions/failsafe/logging/sim; frontend state) against the adopted directive. Evidence cited as `file:line` was verified by the auditors at audit time.

**Airframe decision (owner, 2026-07-22):** ArduPlane stays primary (matches the real aircraft + all SITL validation); new abstractions must be airframe-aware so ArduCopter can be added without redesign. Confirm the eventual spray airframe with Caleb.

---

## Scorecard

| Directive area | Verdict | Summary |
|---|---|---|
| Mission transfer protocol | **STRONG** | MISSION_ITEM_INT both directions, answers MISSION_REQUEST & _INT, retransmit-safe seq replies, ACK close-out both paths (`vehicle_manager.py:701-810`) |
| Command ACK handling | **GOOD, gaps** | arm/disarm/mode wait for COMMAND_ACK and surface real result; GET_HOME, PARAM_SET, MISSION_SET_CURRENT are fire-and-forget |
| Heartbeat management | **GOOD, gaps** | 1 Hz GCS TX, 5 s RX watchdog, auto-reconnect (20×5 s); no independent TX timer, no graded levels |
| Connection state | **ABSENT** | Booleans (`connected`, `reconnecting`), no state machine, no transition events; no SYNCHRONIZING/DEGRADED equivalents |
| Message coverage | **PARTIAL** | Missing entirely: **STATUSTEXT** (vehicle errors silently dropped), **EKF_STATUS_REPORT**, BATTERY_STATUS, AUTOPILOT_VERSION, LOCAL_POSITION_NED |
| Vehicle state model | **PARTIAL** | Typed dataclass with 24 fields; missing `ekfHealthy`, `linkQuality`, `lastHeartbeat`; `connected` only on WS path; unlocked cross-thread reads (torn snapshots possible) |
| Multi-GCS / sysid | **ABSENT** | Own sysid defaults to 255 (collides with Mission Planner), no RX sysid filtering, no routing. MP co-connect only safe on a separate SITL/vehicle endpoint |
| Parameter engine | **WEAK — hardware-blocking** | No cache, no sync-on-connect, no validation, no batch/rollback; writes are fire-and-forget **hardcoded REAL32** with router returning "ok" unconditionally (`vehicle_manager.py:644-654`, `routers/vehicle.py:130-135`) |
| Structured logging | **ABSENT — hardware-blocking** | Zero application logging; connect/loss/commands/params/exceptions all silent, ~12 bare `except: pass` sites. Only JSONL per-flight telemetry recorder exists (good, but it's a flight recorder, not an ops log) |
| Failsafe architecture | **GOOD** (Guardian, 2026-08-14) | ArduPilot onboard failsafes stay primary; `app/guardian.py` adds the GCS-side second layer: GPS/battery/link monitors, RTL energy-margin estimate, NORMAL→WARNING→RTL_REQUESTED→RTL_ACTIVE→LANDING→DISARMED emergency state machine, guardian-commanded RTL with the trigger recorded first, operator-override standdown. Config via `/api/safety/guardian`; verdicts in snapshot/WS. Proven live by the `guardian` scenario (autopilot failsafe off, guardian alone RTLs the plane) |
| Mission model | **PARTIAL** | Typed items but no mission entity (id/name/metadata), no backend persistence, no resume-from-seq-N |
| Coverage planner | **GOOD, gaps** | Polygon/concave, swath, serpentine, keepout clipping with independent-verified buffers, multi-field tour. Missing: headlands, overlap analysis, resume, terrain hooks (constant AGL only) |
| SITL integration | **GOOD** (M4, 2026-08-14) | Bundled SITL + lifecycle API; scenario spawn options (speedup / fresh-eeprom isolation); fault-injection endpoint (gps/battery/gcs_link); 6 scripted scenarios incl. a full autonomous flight, run against the real SITL via `scenarios.ps1` / `pytest -m sitl` |
| Spraying layer | **ABSENT** (known) | No flow/section/rate control, no as-applied verification — Phase 2 per roadmap |
| Frontend discipline | **GOOD, gaps** | UI never parses MAVLink, all commands via backend REST (+1 RC-override WS). But safety logic lives client-side: pre-flight checklist, throttle interlock, alert thresholds, RTL margin. Param edits effectively unvalidated. RCPanel swallows errors and flashes optimistic success |
| Config management | **ABSENT** | Hardcoded constants; one env var (`ORDERS_DB`); no settings file |

## Hardware-blocking findings (fix before the Cube bench test)

**All four cleared as of 2026-07-24** — #1 & #3 by M1a, #2 by M1b, #4 by M2. Retained below for provenance.

1. **STATUSTEXT is dropped.** Every ArduPilot prearm failure, failsafe announcement, and error string is invisible to the operator and to logs. We already paid for this once (fence-breach RTL diagnosed by attaching a second GCS). Directive: "Is state clearly observable from telemetry?" — currently no.
2. **Param writes are unverified fire-and-forget with a hardcoded REAL32 type.** SafetyPanel "applies" failsafes and the router reports success regardless of what the FC accepted. On real hardware that means believing a fence/failsafe is set when it may not be.
3. **No application logging.** A bench-test anomaly is currently undiagnosable after the fact ("Are errors diagnosable from logs?" — no).
4. **Sysid 255 + no RX filtering.** The moment Mission Planner shares an endpoint, the two GCSs collide and either can consume the other's ACKs/PARAM_VALUEs.

## Roadmap (safety-first, incremental — 112-test suite green at every step)

- **M1 — Observability & truth**: ~~structured JSON event log (connection/commands+ACKs/params/mode/arm/failsafe/exceptions, JSONL); STATUSTEXT capture → telemetry + alerts + log; EKF_STATUS_REPORT + SYS_STATUS health bits → `ekf_healthy`; `link_quality`/`last_heartbeat_age` in snapshot; thread-safe snapshot reads; `connected` in GET telemetry.~~ **M1a SHIPPED 2026-07-23** — `app/eventlog.py` + instrumented `vehicle_manager`, `/api/logs/events`, `snapshot()`; 133 tests (21 new); SITL-validated live (EKF alignment STATUSTEXT stream captured, `ekf_healthy` False→True across GPS convergence, sensor `gps` unhealthy→recover + persistent `terrain` correctly flagged, full link→mode→arm→disarm audit trail on disk). ~~**M1b:** param write = read-type → PARAM_SET with correct type → verify PARAM_VALUE echo → report actual accepted value.~~ **M1b SHIPPED 2026-07-24** — `set_param`/`set_params` now read the real MAV_PARAM_TYPE, PARAM_SET with it, wait for the echo (read-back fallback for a dropped echo), and return `{verified, accepted, previous}`; POST `/vehicle/params` + `/safety/geofence` + `/safety/failsafe` return **502 with the failed param(s)** instead of a blind `ok`; SafetyPanel/ParamsPanel name the rejected param and show the value the FC actually holds. 147 tests (14 new, `test_m1b_params.py`); **SITL-validated live** (mixed INT8/REAL32 geofence batch verified + read-back-confirmed on the FC, single float/int writes verified, unknown param → 502 `no PARAM_VALUE echo`, full audit trail in the event log; per-param verify timeout tuned to bound a rejected-write to ~5s). **Next: M2 — link & identity** (or rebuild the exe with M1 first).
- ~~**M2 — Link & identity**: connection state machine … graded link levels; own sysid configurable (default ≠255), RX sysid/compid filtering; AUTOPILOT_VERSION capability fetch; BATTERY_STATUS.~~ **M2 SHIPPED 2026-07-24** — `app/config.py` (GCS_SYSID default **252**, env-configurable); `LinkState` machine DISCONNECTED→CONNECTING→WAITING_HEARTBEAT→SYNCHRONIZING→READY↔DEGRADED→LOST with logged transitions in snapshot/WS; graded `link_level` (good/nominal/degraded/poor/critical from heartbeat age); RX sysid filter in the telemetry loop **and** `_recv_blocking` (drops foreign-system traffic so it can't feed the watchdog or steal ACKs); AUTOPILOT_VERSION capability decode on connect; BATTERY_STATUS consumed/remaining. **Closes hardware-blocking finding #4.** On connect, aligns the vehicle's commanding-GCS param to our sysid via a verified M1b write — firmware-aware (`MAV_GCS_SYSID` on 4.5+, falls back to `SYSID_MYGCS`). 160 tests (+13, `test_m2_link.py`). **SITL-validated (ArduPlane 4.8.0):** full state-machine transition log, `MAV_GCS_SYSID` aligned 255→252 (verified), capabilities fetched, BATTERY_STATUS live, RX filter dropped 944 non-vehicle msgs, and **armed AUTO takeoff held 40m for 24s with NO FS_GCS RTL** (the historical failure mode does not recur on the new sysid). Note: SITL's separate ports (5760/5762) can't reproduce a *shared-endpoint* co-GCS collision, so cross-GCS filtering is covered by unit tests, not the SITL run.
- ~~**M3 — Parameter engine**: cache + full sync on connect, missing-index re-request, metadata validation (ranges/types), batch set with rollback, change tracking.~~ **M3 SHIPPED 2026-07-24** — param cache (name→value/type/index) kept live by every verified write; **background full sync on connect** with **missing-index gap-fill re-request** (`param_sync` progress in snapshot); `app/param_meta.py` validation (finite, int-type fit from the cached MAV_PARAM_TYPE, curated ranges) rejecting bad writes at the API (**422**, never sent); **`set_params_atomic`** all-or-nothing batch with **rollback to previous values** on any mid-batch failure — now backing the geofence/failsafe endpoints; change tracking in the event log (`set` logs previous→accepted, `atomic_ok`/`atomic_rollback`/`set_rejected`). New endpoints `GET /vehicle/params?cached=true` + `POST /vehicle/params/sync`; ParamsPanel loads from cache instantly and surfaces 422s. 174 tests (+14, `test_m3_params.py`). **SITL-validated (ArduPlane 4.8.0):** 1430-param background sync (gap-fill recovered a dropped index live), instant cached read (0.07s), 422 on out-of-range + on a fractional value for an int param (caught via the cached type), atomic geofence apply verified + read-back-confirmed. Full apm.pdef.xml metadata ingestion left as a future enhancement (curated ranges cover the params the app writes).
- ~~**M4 — SITL scenario harness**: reproducible scenarios (field-test, link-loss, gps-failure via SIM_GPS_DISABLE, battery-fault via SIM_BATT_VOLTAGE, rtl-recovery) as scripted backend runs + CI-able integration tests; fault injection endpoints in sim router.~~ **M4 SHIPPED 2026-08-14** — pytest infra (`pytest.ini`, markers `sitl`/`slow`, default run excludes SITL; the 174-test unittest suite collects unchanged); sim router grew **scenario spawn options** (`POST /api/sim/start {speedup, fresh_eeprom}` — fresh_eeprom boots SITL from firmware defaults in an isolated `sitl/_scenario/` scratch dir so the demo eeprom is never touched) and **fault injection** (`POST /api/sim/fault`: `gps` → `SIM_GPS1_ENABLE`/`SIM_GPS_DISABLE` firmware-aware, `battery` → `SIM_BATT_VOLTAGE` sag with restore-on-clear, `gcs_link` → suppresses our own 1Hz heartbeat via `vehicle_manager.set_gcs_heartbeat_suppressed`, cleared automatically on every connect) — all param faults go through the verified M1b write path and land in the event log; **six scenarios in `tests/sitl/`** driving the real FastAPI app (TestClient) against the real bundled SITL: **field-test** (complete autonomous flight from a fresh eeprom: takeoff→waypoints→RTL→auto-land→self-disarm, zero stick input), **link-loss** (GCS silence → vehicle self-RTLs, obeys again on recovery; runs at speedup 1 because our wall-clock heartbeats vs sim-time FS_GCS timing), **gps-failure** (fix collapse surfaced truthfully, link unshaken, recovers), **battery-fault** (sag below threshold → RTL), **rtl-recovery** (commanded abort converges home, mission resumes at the aborted item), **link-watchdog** (SITL killed under the backend → LOST → auto-reconnect self-heals to READY — first time proven against a real dying process). Named entry points per the directive: `backend\scenarios.ps1 field-test|link-loss|gps-failure|battery-fault|rtl-recovery|link-watchdog|all` (= `pytest -m sitl`). **All 6 validated live 2026-08-14, serially, 124s total.** Harness immediately caught a real bug, fixed: vehicle dying mid-param-sync killed the sync thread with `param_sync.syncing` stuck true forever (now fails visibly: `sync_failed` event + clean state). 186 unit tests (+12, `test_m4_sim.py`).
- **GUARDIAN (directive "Failsafe architecture") — SHIPPED 2026-08-14**: `app/guardian.py` (pure logic, fully unit-tested) + 1Hz supervisor thread in `vehicle_manager` + `GET/POST /api/safety/guardian` (partial updates, threshold-order validation). Monitors (armed-gated): link (graded level warn), GPS (fix loss / thin sats — warn-only by default: RTL without GPS can't navigate), battery (warn + sustained-low RTL, debounced like BATT_LOW_TIMER so throttle sag can't trigger), RTL energy margin (pack-capacity + consumption + distance-home → time margin; needs `pack_capacity_mah` configured). Emergency state machine NORMAL→WARNING→RTL_REQUESTED→RTL_ACTIVE→LANDING→DISARMED (RTL_ACTIVE reflects vehicle truth whoever commanded it; LANDING via the land() flow flag; DISARMED sticky post-flight). Guardian RTL: trigger recorded BEFORE the command (directive), verified set_mode, 3-attempt cap, **operator override = guardian stands down** until the condition clears (never fights the pilot). Verdicts ride in `snapshot()['guardian']` → telemetry WS. 209 unit tests (+23 `test_guardian.py`). **SITL-proven**: `guardian` scenario — ArduPilot battery failsafe disabled, pack sagged to 10.3V, guardian debounced 3s, logged `rtl_triggered` (reason: battery), commanded RTL, state machine tracked RTL_ACTIVE. All 7 scenarios green serially (137s).
- **M5 — Mission model & resume**: mission entity (id/name/metadata) + backend persistence; resume-after-interruption (MISSION_SET_CURRENT from tracked seq); headlands + overlap analysis in planner; terrain-frame abstraction (airframe/frame-aware, copter-ready).
- **M6 — Safety logic to backend**: ~~pre-flight checklist evaluated server-side (UI renders verdicts)~~, alert rules + thresholds unified with FS params, ~~RTL margin computed backend-side~~ (guardian), ~~param range validation at the API~~ (M3). **Pre-flight gate SHIPPED 2026-08-14**: `app/preflight.py` (pure evaluation) + `GET /api/safety/preflight`; **arm/takeoff endpoints refuse (409, blockers named) while a blocker fails** — blockers: link READY, GPS 3D, EKF healthy, home known; advisories (battery, RC seen, fence enabled, sensors healthy) inform but never block. Explicit `override:true` bypasses the gate and is logged (`preflight/override` WARN), as is every refusal (`preflight/blocked`). LaunchControl's OVERRIDE toggle now rides through to the backend and its error toast names the failing blockers. 218 unit tests (+9 `test_preflight.py`); `preflight` scenario live-proves the 409-then-arm sequence. Remaining M6 slice: alert-rule unification (UI AlertCenter thresholds vs guardian config) — natural part of the UI-track redesign.
- **M7 — Layer restructure (continuous)**: as each milestone touches `vehicle_manager.py` (815 lines), extract into packages mirroring the directive layout (`mavlink/` transport+ack+heartbeat, `vehicle/` state machine, `telemetry/`, `params/`, `missions/`, `failsafe/`, `sim/`) — strangler moves, never big-bang.
- **Phase 2+ (hardware-gated)**: spraying layer (DO_SET_SERVO/RELAY, section control, as-applied), companion-computer deployment profile, fleet/sysid-per-vehicle.

## Retained strengths (don't regress)

Link lock discipline + `_recv_blocking` heartbeat preservation; threadpool endpoint policy (no blocking pymavlink on the event loop); mission seq-protocol retransmit handling; telemetry reset on connect; auto-reconnect with operator-disconnect distinction; force-arm/AUTO SITL flow; keepout clipping verified against an independent distance implementation; frontend ErrorBoundary + self-healing WS.
